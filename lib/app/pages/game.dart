import 'package:flutter/material.dart';

class Game extends StatelessWidget {
  const Game(final String id, {super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
